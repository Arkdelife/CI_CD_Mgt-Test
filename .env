# Fake credentials, committed on purpose as the deny-rule fixture.
# review_cli.py / compare_tools.py deliberately do NOT load this file — see
# their load_dotenv(REPO / ".env.local") call. A real key belongs in
# .env.local (gitignored), never here.
ANTHROPIC_API_KEY=sk-ant-fake-do-not-use-0000000000000000000000000000
